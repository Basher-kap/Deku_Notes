name: Preview Build

jobs:
  build_android:
    name: Build Android APK
    type: build
    params:
      platform: android
      profile: preview

  build_ios:
    name: Build iOS (ad hoc)
    type: build
    params:
      platform: ios
      profile: preview